name: test-python-script
desc: testing
cmd: python main.py --data ${DATA} --result ${OUTPUT}
env:
  python: auto # передать в DataSphere код и зависимости pip
inputs:
  - numbers.txt: DATA
outputs:
  - result.txt: OUTPUT
cloud-instance-type: c1.4